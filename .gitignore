# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Logs
*.log

# Local runtime data — the app stores JSON under %LOCALAPPDATA%\TeamScheduler,
# never inside the repo, but ignore any stray local data/ folder just in case
TeamScheduler/data/
